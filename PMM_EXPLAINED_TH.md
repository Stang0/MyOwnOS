# การจัดการหน่วยความจำจริง (Physical Memory Manager - PMM)

เอกสารนี้อธิบายถึงระบบ **Physical Memory Manager (PMM)** ที่เราเพิ่งนำเข้ามาใส่ใน MyOS โดยใช้วิธี **Bitmap Allocator** ครับ

---

## 1. PMM คืออะไร และช่วยเรื่องอะไร?

**Physical Memory Manager (PMM)** คือผู้จัดการส่วนตัวของ RAM ทั้งหมดในเครื่อง มีหน้าที่หลักคือ:
*   **รู้ว่า RAM ตรงไหนว่าง ตรงไหนไม่ว่าง:** เครื่องคอมพิวเตอร์มี RAM กระจัดกระจาย บางส่วนใช้โดย Hardware บางส่วนมีไว้ให้เราใช้
*   **แจกจ่ายที่ดิน (Allocation):** เมื่อ Kernel หรือโปรแกรมอยากได้ RAM มาเก็บข้อมูล PMM จะตัดแบ่งพื้นที่ว่างขนาด 4KB (1 Page) มาให้
*   **รับคืนที่ดิน (Deallocation):** เมื่อใช้เสร็จแล้ว PMM จะรับพื้นที่คืนและทำเครื่องหมายว่า "ว่าง" เพื่อให้คนอื่นมาใช้ต่อได้

### ทำไมต้องใช้ Bitmap Allocator?
เราเลือกใช้วิธี **Bitmap** เพราะประหยัดพื้นที่และเข้าใจง่าย:
*   เราใช้ **1 บิต** เพื่อแทนสถานะของ RAM **1 หน้า (Page = 4096 bytes)**
    *   `0` = ว่าง (Free)
    *   `1` = ถูกใช้แล้ว (Used)
*   **ความประหยัด:** แรม 128 MB ใช้พื้นที่สำหรับตาราง Bitmap แค่ 4 KB เท่านั้น!

---

## 2. สิ่งที่เราทำเพิ่มลงไป

เราได้สร้างระบบที่สามารถ:
1.  อ่านแผนที่หน่วยความจำ (Memory Map) จาก **GRUB/Multiboot 2** เพื่อรู้ว่าแรมช่วงไหนใช้งานได้จริง
2.  จองพื้นที่ (Reserve) สำหรับตัว Kernel เองเพื่อป้องกันไม่ให้เราแจกพื้นที่ทับ Kernel
3.  แจก (Allocate) และคืน (Free) พื้นที่ได้จริง

---

## 3. รายละเอียดไฟล์ที่แก้ไขและสร้างใหม่

### 📂 `kernel/arch/x86_64/multiboot.hpp` (สร้างใหม่)
*   **หน้าที่:** เก็บโครงสร้างข้อมูล (Structure) ตามมาตรฐาน **Multiboot 2**
*   **สำคัญยังไง:** ใช้เป็นลายแทงเพื่อคุยกับ GRUB ดูว่า "Memory Map Tag" อยู่ตรงไหน เพื่อให้รู้ตำแหน่งของ RAM

### 📂 `kernel/mm/pmm.hpp` & `.cpp` (หัวใจหลัก)
*   **หน้าที่:** เป็นตัวจัดการ PMM Class
*   **ฟังก์ชันสำคัญ:**
    *   `init(multiboot_info)`: วนลูปอ่านข้อมูลจาก Multiboot และสร้าง Bitmap
    *   `allocate_frame()`: ค้นหาบิตที่เป็น `0` ใน Bitmap เปลี่ยนเป็น `1` แล้วส่งคืนที่อยู่ RAM
    *   `free_frame(ptr)`: คำนวณว่าที่อยู่นี้ตรงกับบิตไหน แล้วแก้บิตกลับเป็น `0`
    *   `bitmap[]`: อาร์เรย์ที่เก็บ 0/1 ของแต่ละหน้า

### 📂 `kernel/kernel.cpp` (แก้ไข)
*   เพิ่มโค้ดเรียก `pmm.init()` ตอนเริ่มต้น
*   เพิ่มการทดสอบ `pmm.allocate_frame()` เพื่อดูว่าได้ที่อยู่แรมออกมาจริงๆ (เช่น `0x200000`)

### 📂 `kernel/drivers/serial.hpp` & `console.cpp` (เพิ่มเติม)
*   **หน้าที่:** เพิ่มการส่งข้อความออกทาง **Serial Port (COM1)**
*   **เหตุผล:** หน้าจอ VGA (Console) เลื่อนไปมาได้ยาก การส่ง log ออกทาง Serial ทำให้เราเห็นค่า Address ของหน่วยความจำได้ชัดเจนผ่าน Terminal ของ QEMU

---

## 4. ตัวอย่างการทำงาน

เมื่อเรารัน `make run` ระบบจะทำงานตามลำดับนี้:
1.  **Boot:** GRUB โหลด Kernel และส่งข้อมูล Multiboot Info มาให้
2.  **PMM Init:** `pmm.init()` อ่านข้อมูล พบว่ามี RAM ว่างช่วง `0x100000` ขึ้นไป
3.  **Protection:** PMM จองพื้นที่ช่วงต้นไว้ให้ Kernel (`0x0 - 0x200000`) เพื่อความปลอดภัย
4.  **Allocate Test:**
    *   ขอแรมครั้งที่ 1 -> ได้ `0x200000` (หน้าแรกถัดจากเคอร์เนล) -> บิตที่เกี่ยวข้องถูกเซตเป็น 1
    *   ขอแรมครั้งที่ 2 -> ได้ `0x201000` (ถัดไปอีก 4KB)
    *   ขอแรมครั้งที่ 3 -> ได้ `0x202000`
5.  **Free Test:**
    *   คืนแรม `0x201000` -> บิตที่เกี่ยวข้องถูกแก้กลับเป็น 0
    *   ขอแรมใหม่ -> ได้ `0x201000` อีกครั้ง (เพราะมันว่างแล้ว)

